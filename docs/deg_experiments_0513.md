# DEG 相关实验汇总 — 2026-05-13

> **数据来源说明**
>
> - ✅ **今日跑出**：结果直接来自 2026-05-13 终端输出，可信
> - ⚠️ **旧数据**：结果来自 2026-03 前的运行记录（`docs/项目回顾.md`），56 天前，已作废

---

## 实验总览（全部 2026-05-13 结果）

| # | 实验名称 | 方法 | 有效扰动数 | STATE+MMD | Baseline | 随机基线 |
|---|---------|------|-----------|-----------|----------|---------|
| 1 | DEG Pearson pval<0.20 | BH 筛 DEG（宽），全部 DEG 算 PCC | — | -0.035 ⚠️旧 | -0.021 ⚠️旧 | ≈0 |
| 2 | DEG Pearson pval<0.05 | BH 筛 DEG（严），全部 DEG 算 PCC | 39/68 (57%) | **0.041** | 0.046 | ≈0 |
| 3 | Top-k PCC 曲线（k=5）| \|scores\| 排序，各 k 独立算 PCC | 67/68 (99%) | **+0.119** | **-0.055** | ≈0 |
| 4 | DES 基因重叠率 | top-50 预测 vs top-50 真实 DEG 交集 | 44/68 (65%) | **0.0014** | **0.0064** | ~0.0028 |

> ⚠️ **DES 旧数据（STATE+MMD=0.0054，Baseline=0.0042）已被今日重跑推翻，见 Slide 6 分析。**

---

---

## Slide 1 — 标题

**DEG 相关实验汇总**

**副标题**：四组实验从不同角度量化扰动方向预测能力

**数据**：HepG2 零样本验证集，68 种基因敲除扰动，18,080 个基因

---

## Slide 2 — 实验一：DEG Pearson（pval < 0.20，谢老师原始方法）⚠️ 旧数据

### 内容

**目的**：用统计显著差异基因（DEG）上的 Pearson r 衡量扰动方向预测能力，替代被否定的全基因组 Pearson r。

**方法**：

```
Step 1  Wilcoxon 秩和检验 + BH 校正
        筛出 adj p < 0.20 的 DEG（每个扰动约 30~200 个基因）

Step 2  模型推理
        对照组细胞 → 平均 CLS → decode → pred_ctrl[18080]
        扰动组细胞 → 平均 CLS → decode → pred_pert[18080]
        pred_logFC = pred_pert - pred_ctrl

Step 3  在 DEG 子集上计算 PCC
        pearsonr(pred_logFC[DEG], actual_logFC[DEG])

Step 4  对 67 个扰动取均值
```

**结果**（⚠️ 旧数据，2026-03）：

| 模型      | Mean PCC   | 正相关比例 |
| --------- | ---------- | ---------- |
| STATE+MMD | **-0.035** | ~48%       |
| Baseline  | **-0.021** | ~48%       |
| 随机期望  | ≈ 0        | 50%        |

**结论**：两者均接近随机水平，初步判断模型对扰动方向无预测能力。

### 讲稿

> 这是谢老师提出的评估方法，也是整个评估体系的起点。全基因组 Pearson r = 0.70 被否定后，我们改为只在显著差异的基因上算相关性。阈值选 adj p < 0.20，是因为 HepG2 每种扰动的细胞数不多（几十到几百），更严格的阈值会筛出太少的基因。
>
> 结果两个模型都约等于 0，看起来方向预测能力都是随机水平。但后续实验会揭示这个结论是不完整的——问题在于阈值方法把信号和噪声一起平均了。

---

## Slide 3 — 实验二：DEG Pearson（pval < 0.05，更严格阈值）✅ 今日

### 内容

**目的**：验证用更严格的统计阈值（adj p < 0.05）是否能减少弱 DEG 噪声、提升评估质量。

**方法**：与实验一相同，仅将阈值从 0.20 改为 0.05。

**结果**（✅ 2026-05-13 跑出）：

| 模型      | Mean PCC | Std    | 有效扰动数 | 正相关比例 |
| --------- | -------- | ------ | ---------- | ---------- |
| STATE+MMD | 0.0411   | 0.2049 | **39/68**  | 53.8%      |
| Baseline  | 0.0460   | 0.2989 | **39/68**  | 59.0%      |

**关键发现**：

```
68 个扰动中，有 29 个（43%）因 DEG 数量不足被跳过
被跳过的 = 差异较弱、较难预测的扰动
留下的 39 个 = 差异最显著、最"容易"的扰动

→ pval 阈值方法通过"跳过难例"系统性地选了简单情况
→ 在这 39 个容易的扰动上，两模型表现相近（0.041 vs 0.046）
→ Baseline 略高于 STATE+MMD，看似矛盾
```

**结论**：pval 阈值法评估存在选择偏差——跳过了 43% 的难预测扰动，使得模型间的真实差距被遮蔽。

### 讲稿

> 提高阈值到 0.05，预期是减少噪声、得到更干净的信号。但发现了一个意外问题：68 个扰动里有 29 个无法在 adj p < 0.05 下筛出足够的 DEG，直接被跳过了。
>
> 这 29 个被跳过的扰动是"差异最弱"的那些——它们在 HepG2 的基因表达变化本来就不显著，也是最难预测的扰动。方法 B 只评估了剩下 39 个"容易的"扰动，结果两个模型都差不多（0.041 vs 0.046），甚至 Baseline 略高。
>
> 这不是 Baseline 真的更好，而是因为两个模型在"最容易预测"的扰动上本来就差不多——MMD 对齐的真正价值在于处理"更难"的扰动，而这些扰动被 pval 方法排除掉了。这是 p-value 阈值评估的系统性盲点。

---

## Slide 4 — 实验三：Top-k PCC 曲线 ✅ 今日（主要结果）

### 内容

**目的**：设计一种不受 p-value 阈值局限的评估方法，覆盖全部扰动，同时聚焦于差异最显著的基因。

**方法**：

```
Step 1  对全部 18,080 个基因做 Wilcoxon 检验
        按 |scores| 降序排列（不设阈值，全部基因都参与排序）
        ranked_genes = [最显著, 第二显著, ..., 第18080名]

Step 2  模型推理（同实验一/二）
        pred_logFC_all = pred_pert - pred_ctrl  (18080维)
        actual_logFC_all = actual_pert - actual_ctrl (18080维)

Step 3  for k = 1, 2, ..., 10:
            genes_k = ranked_genes[:k]
            PCC_k = pearsonr(pred_logFC[genes_k], actual_logFC[genes_k])

Step 4  对 67 个扰动取均值 → mean_PCC(k) 曲线
```

**结果**（✅ 2026-05-13，两次运行方向一致）：

| k   | STATE+MMD run1    | STATE+MMD run2    | Baseline run1     | Baseline run2      |
| --- | ----------------- | ----------------- | ----------------- | ------------------ |
| 2   | 0.134（artifact） | 0.224（artifact） | 0.015（artifact） | -0.045（artifact） |
| 3   | **+0.096**        | **+0.099**        | -0.112            | -0.156             |
| 4   | **+0.102**        | **+0.088**        | -0.036            | -0.079             |
| 5   | **+0.126**        | **+0.119**        | -0.021            | -0.055             |
| 6   | **+0.115**        | **+0.080**        | -0.026            | -0.044             |
| 7   | +0.088            | +0.033            | -0.013            | -0.044             |
| 8   | +0.052            | +0.008            | +0.020            | -0.035             |
| 9   | +0.032            | +0.001            | -0.001            | -0.045             |
| 10  | +0.053            | +0.032            | +0.014            | -0.032             |

> k=1：NaN（1 个点无法计算 Pearson r）
> k=2：pearsonr 恒为 ±1（2 点确定直线，数学 artifact），两次运行符号不同，说明不稳定，忽略

> **两次运行细微差异来源**：每次随机采样最多 200 个细胞，采样结果略有不同。但 k=3~6 区间的**方向完全一致**（STATE+MMD 正，Baseline 负），结论稳定。

**正相关扰动比例（Fraction PCC > 0，run2 数据）：**

| k   | STATE+MMD | Baseline | 随机期望 |
| --- | --------- | -------- | -------- |
| 3   | ~55%      | ~42%     | 50%      |
| 4   | ~57%      | ~46%     | 50%      |
| 5   | ~60%      | ~46%     | 50%      |
| 6   | ~60%      | ~51%     | 50%      |

**结论**：

1. STATE+MMD 在 k=3~6 的 mean PCC 约 +0.09~+0.12，**始终正值**
2. Baseline 在 k=3~7 的 mean PCC **全部为负**（-0.156 ~ -0.013），在最显著基因上方向系统性预测错误
3. 信号随 k 增大衰减：弱 DEG 对方向预测贡献的是噪声
4. 两次运行结果方向完全一致，k=2 不稳定可忽略

### 讲稿

> Top-k 方法的核心设计思路是：不设统计阈值，对全部 18,080 个基因按 Wilcoxon |scores| 排序，然后从最显著的 1 个基因开始，逐步加入更多基因，看 PCC 怎么变化。
>
> 这个方法有两个优点：第一，覆盖全部 67 个扰动（不跳过任何难例）；第二，通过 k 的变化揭示"信号在哪里"。
>
> 结果非常清晰：STATE+MMD 在 k=3~6 的区间，mean PCC 稳定在 +0.09 到 +0.12。Baseline 同样区间全部是负数，k=3 时达到 -0.156——这不是"预测不准"，而是"系统性地预测反了"。
>
> 两次运行的 k=2 结果差异很大（0.134 vs 0.224；Baseline 0.015 vs -0.045），这是因为 k=2 时 pearsonr 必然等于 ±1，结果完全取决于这 2 个基因的方向是否一致，随机性很大，应该忽略。k=3 以后的结果在两次运行中方向完全一致，结论可靠。
>
> PCC 随 k 增大下降这个趋势也是重要信息：模型的方向预测信号集中在最显著的少数基因上，加入排名靠后（差异弱）的基因只会引入噪声。

---

## Slide 5 — 三种 DEG Pearson 方法的直接对比

### 内容

**同一批数据，三种评估方法，结论截然不同：**

| 方法               | 筛基因方式               | 覆盖扰动数       | STATE+MMD  | Baseline   | 结论                      |
| ------------------ | ------------------------ | ---------------- | ---------- | ---------- | ------------------------- |
| pval < 0.20 ⚠️     | BH 校正筛                | 67/68            | -0.035     | -0.021     | 两者均≈0                  |
| pval < 0.05 ✅     | BH 校正筛（严）          | **39/68（57%）** | 0.041      | 0.046      | 两者均≈0，Baseline 略高   |
| **Top-k（k=5）✅** | \|Wilcoxon scores\| 降序 | **67/68（99%）** | **+0.119** | **-0.055** | **MMD 显著优于 Baseline** |

**为什么同样的数据得出不同结论？**

```
pval 方法：
  把 DEG 里所有基因一视同仁地平均
  弱 DEG（adj p 刚过阈值）的 logFC 接近 0，方向不确定
  这些弱基因的随机噪声把强基因的真实信号稀释至 0

  此外，pval<0.05 还跳过了 43% 的难预测扰动（选择偏差）

Top-k 方法：
  只取 |scores| 最大的前 k 个基因
  这些基因差异最显著，logFC 方向最可靠
  噪声被排除在外，真实信号得以显现
```

**核心结论**：评估方法的选择，决定了我们能不能看到模型的真实能力。

### 讲稿

> 把三组实验放在一起，最重要的发现是：同样的模型、同样的数据，用不同的评估方法，得出的结论完全不同。
>
> pval < 0.20 和 pval < 0.05 这两种方法都显示两个模型差不多、都接近 0。但 Top-k 方法显示 STATE+MMD 在 k=5 时 PCC = +0.119，Baseline 是 -0.055，差距约 0.17。
>
> 原因有两层。第一层是"信号稀释"：pval 方法把弱 DEG 和强 DEG 一视同仁，弱 DEG 的方向接近噪声，平均之后把强 DEG 的信号淹没了。第二层是"选择偏差"：pval < 0.05 方法跳过了 43% 最难预测的扰动，只评估容易的案例，在这些容易的案例上两个模型本来就差不多，所以看不出 MMD 的优势。
>
> Top-k 方法避免了这两个问题：它覆盖全部 67 个扰动，也只关注每个扰动里差异最显著的少数基因。这让我们第一次清楚地看到 STATE+MMD 和 Baseline 的真实差距。

---

## Slide 6 — 实验四：DES 基因重叠率 ✅ 今日重跑

### 内容

**目的**：从"基因命中率"角度评估扰动预测能力——不看方向，只看有没有预测到正确的基因。

**方法**：

```
真实端：Wilcoxon + BH（adj p < 0.05）筛 DEG，
        取 |Wilcoxon scores| 最大的 top-50 = 真实变化最显著的基因

预测端：取 |pred_logFC| 最大的 top-50 = 模型认为变化最大的基因

DES = |预测 top-50 ∩ 真实 top-50| / 50

随机基线 = (50 × 50) / 18080 ≈ 0.0028
有效扰动：adj p < 0.05 下 DEG 数 ≥ 3 的扰动，共 44/68（65%）
```

**结果**（✅ 2026-05-13 重跑）：

| 模型 | Mean DES | Median | Std | N | DES>0 比例 | vs 随机 |
|------|---------|--------|-----|---|-----------|--------|
| STATE+MMD | **0.0014** | 0.0000 | 0.0066 | 44 | **4.5%** | **低于随机** |
| Baseline | **0.0064** | 0.0000 | 0.0180 | 44 | **15.9%** | 高于随机 |
| 随机基线 | ~0.0028 | — | — | — | — | — |

**与旧结果对比**（旧：STATE+MMD=0.0054 > Baseline=0.0042）：

> 排序依据已修改：旧版用 `|logfoldchanges|`，新版改为 `|Wilcoxon scores|`，选出的"真实 top-50 DEG"不同，两次结果不可直接比较，旧结果作废。

**关键发现**：

```
STATE+MMD DES = 0.0014 < 随机基线（0.0028）
  → 模型预测的"最可能变化的基因"与真实 DEG 重叠率低于随机
  → DES>0 只有 4.5%（44 个扰动中仅 2 个命中）

Baseline DES = 0.0064 > 随机（0.0028）
  → 在"预测哪些基因变化"这一任务上，Baseline 反而更好
  → DES>0 有 15.9%（7 个扰动命中）

⚠️ 这与 Top-k PCC 结论相反：
  Top-k PCC → STATE+MMD 方向预测优于 Baseline
  DES       → Baseline 基因命中率优于 STATE+MMD
```

**结论**：DES 和 Top-k PCC 测量的是扰动预测的两个独立维度，MMD 对它们的影响方向相反。

### 讲稿

> DES 和 PCC 问的是两个不同的问题：PCC 问"你知道这些基因该往哪个方向变化吗"，DES 问"你知道是哪些基因会变化吗"。一个模型可以在一个维度表现好，在另一个上表现差。
>
> 今日重跑的结果令人意外：STATE+MMD 的 DES 均值是 0.0014，比随机基线 0.0028 还低；Baseline 是 0.0064，是随机基线的两倍多。这和 Top-k PCC 的结论完全相反——Top-k PCC 显示 STATE+MMD（k=5：+0.119）明显优于 Baseline（-0.055）。
>
> 一个可能的解释是：MMD 域对齐迫使模型把三个训练细胞系的 embedding 压缩到同一个区域，这个操作改变了 binary_decoder 对不同基因的"激活权重"。融合之后，binary_decoder 倾向于激活三个细胞系共有的稳定基因，而不是 HepG2 特异性的差异基因。Baseline 没有这种 embedding 压缩，binary_decoder 的基因激活更分散，反而更容易碰到真实 DEG。
>
> 但这是推测。关键结论是：**MMD 对齐是一把双刃剑——它改善了最强 DEG 的方向预测（PCC），但同时降低了基因选择的命中率（DES）。** 两个指标的分歧提示我们，模型改进不能只看一个维度。

### 老师可能的问题

**Q：STATE+MMD DES 低于随机，这怎么理解？**

> 随机基线 0.0028 是从 18080 个基因里随机挑 50 个的期望命中率。STATE+MMD 是 0.0014，低于这个值，意味着它预测的 top-50 基因是在"主动回避"真实 DEG——当然不是主动，而是 MMD 对齐后 binary_decoder 的激活偏向了一批稳定的、跨细胞系共有的基因，这些基因恰好不在每个扰动的 DEG 里。注意均值 0.0014 的 std=0.0066 很大，只有 2 个扰动命中，结论需要更多实验确认。

**Q：为什么今日和旧数据方向相反？**

> eval_des.py 被修改过：旧版用 `|logfoldchanges|` 对真实 DEG 排序选 top-50，新版改为 `|Wilcoxon scores|`。两种排序选出的基因集合不同，DES 分母改变，结果不可直接比较。这提示我们评估方法本身的稳定性值得关注——不同的"真实 top-k"定义会导致不同的结论。

---

## Slide 7 — 综合结论

### 内容

**四组实验的统一解读：**

```
信号确实存在，但稀疏
  └→ 集中在每个扰动里 |Wilcoxon scores| 最大的 3~6 个基因上
  └→ 对弱 DEG 没有方向预测能力（k>7 后 PCC 趋向 0）

MMD 域对齐是一把双刃剑
  └→ 方向预测（Top-k PCC）：STATE+MMD > Baseline
       k=3：+0.099 vs -0.156，差距 0.26
       → MMD 纠正了 Baseline 在最强 DEG 上的系统性反向
  └→ 基因命中率（DES）：STATE+MMD < Baseline
       STATE+MMD=0.0014 < 随机（0.0028），Baseline=0.0064 > 随机
       → MMD 对齐压缩了 embedding，binary_decoder 转向跨细胞系稳定基因
         而非 HepG2 特异性 DEG，导致基因选择命中率下降

DES 与 PCC 测量的是独立的两个维度
  └→ PCC：知道哪些基因该往哪个方向变？
  └→ DES：知道是哪些基因会变？
  └→ 一个模型可以在方向上好（STATE+MMD），在命中上差；反之亦然

评估方法的选择决定能否看到信号
  └→ pval 阈值方法：信号被稀释 / 选择偏差遮蔽 → 结论"无能力"
  └→ Top-k 方法：聚焦强 DEG，覆盖全部扰动 → 结论"有限但真实的方向能力"
```

**下一步**：提高 logfc_weight（0.1 → 0.5），让方向损失梯度真正影响 binary_decoder，同时用 Top-k PCC + DES 双指标评估——两个维度的变化方向需要一起监控。

### 讲稿

> 综合四组实验，我们现在对模型能力有了更清晰、也更复杂的认识。
>
> 首先，模型不是完全没有方向预测能力。Top-k PCC 曲线明确显示了信号：STATE+MMD 在 k=3~6 的 mean PCC 稳定在 +0.09 到 +0.12，而 Baseline 同样区间全部是负数。这个信号是稀疏的，集中在最显著的少数基因上，对弱差异基因几乎没有预测能力。
>
> 其次，MMD 域对齐的作用是双刃剑，不能简单说"有效"或"无效"。在方向预测维度（PCC），MMD 明显有效——它把 Baseline 在最强 DEG 上的系统性反向（k=3：-0.156）纠正成了正向（+0.099）。但在基因命中率维度（DES），MMD 反而有害——STATE+MMD 的 DES 均值（0.0014）甚至低于随机基线（0.0028），而 Baseline 的 DES（0.0064）高于随机。一个可能的机制是：MMD 把三个训练细胞系的 CLS embedding 压缩到同一区域，迫使 binary_decoder 聚焦于跨细胞系共有的稳定基因，而这些基因不在 HepG2 的 DEG 里。
>
> 这两个发现告诉我们，模型改进需要双指标追踪。仅用 PCC 可能看不到基因选择的退化；仅用 DES 可能看不到方向预测的进步。下一步提高 logfc_weight，我们需要同时监控 Top-k PCC 和 DES，才能判断改进方向是否真的有效。
>
> 最后，方法论层面：评估方式本身决定了能否发现模型的真实能力。Top-k PCC 是目前最能揭示方向信号的评估方式，应该作为主要指标；DES 提供互补的基因选择视角，两者缺一不可。

### 老师可能的问题

**Q：DES 和 PCC 方向相反，你怎么知道哪个指标更可信？**

> 两个指标都有效，它们测量不同的事情。PCC 测"方向"，DES 测"命中"，两者本来就是正交维度，可以同时成立。类比：一个医生可能知道某种药会让某个指标变高还是变低（方向对），但不一定知道是哪个基因负责这个变化（命中对）。STATE+MMD 的情况是：它知道方向，但不知道是哪些基因。

**Q：MMD 既然降低了 DES，是否应该去掉 MMD？**

> 不应该轻易去掉。首先，Top-k PCC 显示 MMD 对方向预测的贡献非常关键（Baseline 系统性反向），这是核心能力。其次，DES 结果存在不确定性（n=44，std 很大，仅 2 个扰动命中），需要更多实验确认。更有意义的方向是：在保留 MMD 的同时，通过调整 logfc_weight 或 decoder 结构，分别改善两个维度。

---

## 附录：重跑命令

### 实验一（pval < 0.20）重跑

```bash
cd /media/mldadmin/home/s125mdg34_03/state/src

python ../scripts/eval_deg_pearson.py \
    --checkpoint .../checkpoints/mmd_aae/last.ckpt \
    --baseline   .../checkpoints/no_mmd_baseline/last.ckpt \
    --config     ../configs/mmd_aae_config.yaml \
    --pval_cutoff 0.20 \
    --top_k_max  10
```

python ../scripts/eval_deg_pearson.py --checkpoint /media/mldadmin/home/s125mdg34_03/state/checkpoints/mmd_aae/last.ckpt --baseline /media/mldadmin/home/s125mdg34_03/state/checkpoints/no_mmd_baseline/last.ckpt --config ../configs/mmd_aae_config.yaml --top_k_max 10 --pval_cutoff 0.20

### 实验四（DES）重跑

```bash
cd /media/mldadmin/home/s125mdg34_03/state/src

python ../scripts/eval_des.py \
    --checkpoint .../checkpoints/mmd_aae/last.ckpt \
    --baseline   .../checkpoints/no_mmd_baseline/last.ckpt \
    --config     ../configs/mmd_aae_config.yaml
```

## python ../scripts/eval_des.py --checkpoint /media/mldadmin/home/s125mdg34_03/state/checkpoints/mmd_aae/last.ckpt --baseline /media/mldadmin/home/s125mdg34_03/state/checkpoints/no_mmd_baseline/last.ckpt --config ../configs/mmd_aae_config.yaml

## 附录：今日实验完整数字（run2，2026-05-13）

### Top-k PCC — STATE+MMD

| k   | mean PCC | median | std    | N valid |
| --- | -------- | ------ | ------ | ------- |
| 1   | NaN      | NaN    | NaN    | 0       |
| 2   | 0.2239   | 1.0000 | 0.9746 | 67      |
| 3   | 0.0993   | 0.2490 | 0.7152 | 67      |
| 4   | 0.0879   | 0.0975 | 0.5569 | 67      |
| 5   | 0.1185   | 0.1953 | 0.4969 | 67      |
| 6   | 0.0795   | 0.0223 | 0.4313 | 67      |
| 7   | 0.0326   | 0.0085 | 0.4070 | 67      |
| 8   | 0.0081   | 0.0059 | 0.3832 | 67      |
| 9   | 0.0010   | 0.0055 | 0.3511 | 67      |
| 10  | 0.0320   | 0.0363 | 0.3235 | 67      |

### Top-k PCC — Baseline

| k   | mean PCC | median  | std    | N valid |
| --- | -------- | ------- | ------ | ------- |
| 1   | NaN      | NaN     | NaN    | 0       |
| 2   | -0.0448  | -1.0000 | 0.9990 | 67      |
| 3   | -0.1559  | -0.3176 | 0.7132 | 67      |
| 4   | -0.0792  | -0.0978 | 0.5790 | 67      |
| 5   | -0.0552  | -0.0051 | 0.5009 | 67      |
| 6   | -0.0438  | 0.0070  | 0.4309 | 67      |
| 7   | -0.0437  | -0.0429 | 0.4203 | 67      |
| 8   | -0.0349  | 0.0315  | 0.4031 | 67      |
| 9   | -0.0445  | 0.0362  | 0.3729 | 67      |
| 10  | -0.0318  | 0.0595  | 0.3610 | 67      |

### pval < 0.05 — 今日

| 模型      | Mean PCC | Std    | N（有效扰动） | 正相关比例 |
| --------- | -------- | ------ | ------------- | ---------- |
| STATE+MMD | 0.0411   | 0.2049 | 39/68         | 53.8%      |
| Baseline  | 0.0460   | 0.2989 | 39/68         | 59.0%      |
