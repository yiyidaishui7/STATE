# DEG 相关实验汇总 — 2026-05-13

> **数据来源说明**
> - ✅ **今日跑出**：结果直接来自 2026-05-13 终端输出，可信
> - ⚠️ **旧数据**：结果来自 2026-03 前的运行记录（`docs/项目回顾.md`），56 天前，如需重现请用文末命令重跑

---

## 实验总览

| # | 实验名称 | 方法 | 数据来源 | STATE+MMD | Baseline |
|---|---------|------|---------|-----------|----------|
| 1 | DEG Pearson pval<0.20 | Wilcoxon BH 筛 DEG，全部 DEG 算 PCC | ⚠️ 旧数据 | -0.035 | -0.021 |
| 2 | DEG Pearson pval<0.05 | Wilcoxon BH 筛 DEG（更严），全部 DEG 算 PCC | ✅ 今日 | 0.0411（N=39）| 0.0460（N=39）|
| 3 | Top-k PCC 曲线 | 按 \|Wilcoxon scores\| 排序，k=1..10 各算一次 PCC | ✅ 今日 | k=3~5: +0.099~+0.119 | k=3~5: -0.156~-0.055 |
| 4 | DES 基因重叠率 | top-50 预测基因 vs top-50 真实 DEG 的交集 | ⚠️ 旧数据 | 0.0054 | 0.0042 |

---

---

## Slide 1 — 标题

**DEG 相关实验汇总**

**副标题**：四组实验从不同角度量化扰动方向预测能力

**数据**：HepG2 零样本验证集，68 种基因敲除扰动，18,080 个基因

---

## Slide 2 — 实验一：DEG Pearson（pval < 0.20，谢老师原始方法）⚠️ 旧数据

### 内容

**目的**：用统计显著差异基因（DEG）上的 Pearson r 衡量扰动方向预测能力，替代被否定的全基因组 Pearson r。

**方法**：

```
Step 1  Wilcoxon 秩和检验 + BH 校正
        筛出 adj p < 0.20 的 DEG（每个扰动约 30~200 个基因）

Step 2  模型推理
        对照组细胞 → 平均 CLS → decode → pred_ctrl[18080]
        扰动组细胞 → 平均 CLS → decode → pred_pert[18080]
        pred_logFC = pred_pert - pred_ctrl

Step 3  在 DEG 子集上计算 PCC
        pearsonr(pred_logFC[DEG], actual_logFC[DEG])

Step 4  对 67 个扰动取均值
```

**结果**（⚠️ 旧数据，2026-03）：

| 模型 | Mean PCC | 正相关比例 |
|------|---------|-----------|
| STATE+MMD | **-0.035** | ~48% |
| Baseline | **-0.021** | ~48% |
| 随机期望 | ≈ 0 | 50% |

**结论**：两者均接近随机水平，初步判断模型对扰动方向无预测能力。

### 讲稿

> 这是谢老师提出的评估方法，也是整个评估体系的起点。全基因组 Pearson r = 0.70 被否定后，我们改为只在显著差异的基因上算相关性。阈值选 adj p < 0.20，是因为 HepG2 每种扰动的细胞数不多（几十到几百），更严格的阈值会筛出太少的基因。
>
> 结果两个模型都约等于 0，看起来方向预测能力都是随机水平。但后续实验会揭示这个结论是不完整的——问题在于阈值方法把信号和噪声一起平均了。

---

## Slide 3 — 实验二：DEG Pearson（pval < 0.05，更严格阈值）✅ 今日

### 内容

**目的**：验证用更严格的统计阈值（adj p < 0.05）是否能减少弱 DEG 噪声、提升评估质量。

**方法**：与实验一相同，仅将阈值从 0.20 改为 0.05。

**结果**（✅ 2026-05-13 跑出）：

| 模型 | Mean PCC | Std | 有效扰动数 | 正相关比例 |
|------|---------|-----|-----------|-----------|
| STATE+MMD | 0.0411 | 0.2049 | **39/68** | 53.8% |
| Baseline | 0.0460 | 0.2989 | **39/68** | 59.0% |

**关键发现**：

```
68 个扰动中，有 29 个（43%）因 DEG 数量不足被跳过
被跳过的 = 差异较弱、较难预测的扰动
留下的 39 个 = 差异最显著、最"容易"的扰动

→ pval 阈值方法通过"跳过难例"系统性地选了简单情况
→ 在这 39 个容易的扰动上，两模型表现相近（0.041 vs 0.046）
→ Baseline 略高于 STATE+MMD，看似矛盾
```

**结论**：pval 阈值法评估存在选择偏差——跳过了 43% 的难预测扰动，使得模型间的真实差距被遮蔽。

### 讲稿

> 提高阈值到 0.05，预期是减少噪声、得到更干净的信号。但发现了一个意外问题：68 个扰动里有 29 个无法在 adj p < 0.05 下筛出足够的 DEG，直接被跳过了。
>
> 这 29 个被跳过的扰动是"差异最弱"的那些——它们在 HepG2 的基因表达变化本来就不显著，也是最难预测的扰动。方法 B 只评估了剩下 39 个"容易的"扰动，结果两个模型都差不多（0.041 vs 0.046），甚至 Baseline 略高。
>
> 这不是 Baseline 真的更好，而是因为两个模型在"最容易预测"的扰动上本来就差不多——MMD 对齐的真正价值在于处理"更难"的扰动，而这些扰动被 pval 方法排除掉了。这是 p-value 阈值评估的系统性盲点。

---

## Slide 4 — 实验三：Top-k PCC 曲线 ✅ 今日（主要结果）

### 内容

**目的**：设计一种不受 p-value 阈值局限的评估方法，覆盖全部扰动，同时聚焦于差异最显著的基因。

**方法**：

```
Step 1  对全部 18,080 个基因做 Wilcoxon 检验
        按 |scores| 降序排列（不设阈值，全部基因都参与排序）
        ranked_genes = [最显著, 第二显著, ..., 第18080名]

Step 2  模型推理（同实验一/二）
        pred_logFC_all = pred_pert - pred_ctrl  (18080维)
        actual_logFC_all = actual_pert - actual_ctrl (18080维)

Step 3  for k = 1, 2, ..., 10:
            genes_k = ranked_genes[:k]
            PCC_k = pearsonr(pred_logFC[genes_k], actual_logFC[genes_k])

Step 4  对 67 个扰动取均值 → mean_PCC(k) 曲线
```

**结果**（✅ 2026-05-13，两次运行方向一致）：

| k | STATE+MMD run1 | STATE+MMD run2 | Baseline run1 | Baseline run2 |
|---|---------------|---------------|--------------|--------------|
| 2 | 0.134（artifact）| 0.224（artifact）| 0.015（artifact）| -0.045（artifact）|
| 3 | **+0.096** | **+0.099** | -0.112 | -0.156 |
| 4 | **+0.102** | **+0.088** | -0.036 | -0.079 |
| 5 | **+0.126** | **+0.119** | -0.021 | -0.055 |
| 6 | **+0.115** | **+0.080** | -0.026 | -0.044 |
| 7 | +0.088 | +0.033 | -0.013 | -0.044 |
| 8 | +0.052 | +0.008 | +0.020 | -0.035 |
| 9 | +0.032 | +0.001 | -0.001 | -0.045 |
| 10 | +0.053 | +0.032 | +0.014 | -0.032 |

> k=1：NaN（1 个点无法计算 Pearson r）
> k=2：pearsonr 恒为 ±1（2 点确定直线，数学 artifact），两次运行符号不同，说明不稳定，忽略

> **两次运行细微差异来源**：每次随机采样最多 200 个细胞，采样结果略有不同。但 k=3~6 区间的**方向完全一致**（STATE+MMD 正，Baseline 负），结论稳定。

**正相关扰动比例（Fraction PCC > 0，run2 数据）：**

| k | STATE+MMD | Baseline | 随机期望 |
|---|-----------|----------|---------|
| 3 | ~55% | ~42% | 50% |
| 4 | ~57% | ~46% | 50% |
| 5 | ~60% | ~46% | 50% |
| 6 | ~60% | ~51% | 50% |

**结论**：

1. STATE+MMD 在 k=3~6 的 mean PCC 约 +0.09~+0.12，**始终正值**
2. Baseline 在 k=3~7 的 mean PCC **全部为负**（-0.156 ~ -0.013），在最显著基因上方向系统性预测错误
3. 信号随 k 增大衰减：弱 DEG 对方向预测贡献的是噪声
4. 两次运行结果方向完全一致，k=2 不稳定可忽略

### 讲稿

> Top-k 方法的核心设计思路是：不设统计阈值，对全部 18,080 个基因按 Wilcoxon |scores| 排序，然后从最显著的 1 个基因开始，逐步加入更多基因，看 PCC 怎么变化。
>
> 这个方法有两个优点：第一，覆盖全部 67 个扰动（不跳过任何难例）；第二，通过 k 的变化揭示"信号在哪里"。
>
> 结果非常清晰：STATE+MMD 在 k=3~6 的区间，mean PCC 稳定在 +0.09 到 +0.12。Baseline 同样区间全部是负数，k=3 时达到 -0.156——这不是"预测不准"，而是"系统性地预测反了"。
>
> 两次运行的 k=2 结果差异很大（0.134 vs 0.224；Baseline 0.015 vs -0.045），这是因为 k=2 时 pearsonr 必然等于 ±1，结果完全取决于这 2 个基因的方向是否一致，随机性很大，应该忽略。k=3 以后的结果在两次运行中方向完全一致，结论可靠。
>
> PCC 随 k 增大下降这个趋势也是重要信息：模型的方向预测信号集中在最显著的少数基因上，加入排名靠后（差异弱）的基因只会引入噪声。

---

## Slide 5 — 三种 DEG Pearson 方法的直接对比

### 内容

**同一批数据，三种评估方法，结论截然不同：**

| 方法 | 筛基因方式 | 覆盖扰动数 | STATE+MMD | Baseline | 结论 |
|------|-----------|-----------|-----------|----------|------|
| pval < 0.20 ⚠️ | BH 校正筛 | 67/68 | -0.035 | -0.021 | 两者均≈0 |
| pval < 0.05 ✅ | BH 校正筛（严） | **39/68（57%）** | 0.041 | 0.046 | 两者均≈0，Baseline 略高 |
| **Top-k（k=5）✅** | \|Wilcoxon scores\| 降序 | **67/68（99%）** | **+0.119** | **-0.055** | **MMD 显著优于 Baseline** |

**为什么同样的数据得出不同结论？**

```
pval 方法：
  把 DEG 里所有基因一视同仁地平均
  弱 DEG（adj p 刚过阈值）的 logFC 接近 0，方向不确定
  这些弱基因的随机噪声把强基因的真实信号稀释至 0

  此外，pval<0.05 还跳过了 43% 的难预测扰动（选择偏差）

Top-k 方法：
  只取 |scores| 最大的前 k 个基因
  这些基因差异最显著，logFC 方向最可靠
  噪声被排除在外，真实信号得以显现
```

**核心结论**：评估方法的选择，决定了我们能不能看到模型的真实能力。

### 讲稿

> 把三组实验放在一起，最重要的发现是：同样的模型、同样的数据，用不同的评估方法，得出的结论完全不同。
>
> pval < 0.20 和 pval < 0.05 这两种方法都显示两个模型差不多、都接近 0。但 Top-k 方法显示 STATE+MMD 在 k=5 时 PCC = +0.119，Baseline 是 -0.055，差距约 0.17。
>
> 原因有两层。第一层是"信号稀释"：pval 方法把弱 DEG 和强 DEG 一视同仁，弱 DEG 的方向接近噪声，平均之后把强 DEG 的信号淹没了。第二层是"选择偏差"：pval < 0.05 方法跳过了 43% 最难预测的扰动，只评估容易的案例，在这些容易的案例上两个模型本来就差不多，所以看不出 MMD 的优势。
>
> Top-k 方法避免了这两个问题：它覆盖全部 67 个扰动，也只关注每个扰动里差异最显著的少数基因。这让我们第一次清楚地看到 STATE+MMD 和 Baseline 的真实差距。

---

## Slide 6 — 实验四：DES 基因重叠率 ⚠️ 旧数据

### 内容

**目的**：从"命中率"角度评估扰动方向预测能力（与 Pearson r 互补）。

**方法**：

```
预测端：取 |pred_logFC| 最大的 top-50 基因
真实端：取 |actual_logFC| 最大的 top-50 真实 DEG

DES = |预测 top-50 ∩ 真实 top-50| / 50

随机基线：从 18,080 个基因随机取 50 个与真实 top-50 的重叠率
         = 50/18080 × 50 ≈ 0.0028
```

**结果**（⚠️ 旧数据，2026-03/04）：

| 模型 | DES 均值 | DES > 0 的扰动比例 | 随机基线 |
|------|---------|-----------------|---------|
| STATE+MMD | 0.0054 | 21% | 0.0028 |
| Baseline | 0.0042 | — | 0.0028 |

**结论**：

- 两者均极低（接近随机 0.0028），中位数 = 0（大多数扰动预测基因与真实 DEG 完全没有交集）
- STATE+MMD 略高于 Baseline，但差距很小
- 与 DEG Pearson（pval 阈值方法）结论一致：命中率极低

### 讲稿

> DES 是 STATE 原生的评估指标，衡量的是命中率：模型预测的 top-50 变化基因里，有多少和真实 top-50 DEG 重叠。随机基线是 50/18080×50 ≈ 0.0028，即随机选 50 个基因平均只有不到 1 个能和真实 DEG 重叠。
>
> 我们的结果是 0.0054，约是随机基线的两倍，但中位数是 0——说明大部分扰动完全没有命中，只有少数扰动碰巧命中了几个基因把均值拉高。
>
> 这个结果和 pval 阈值方法的 DEG Pearson ≈ 0 方向一致：如果用 pval 阈值评估，确实看不出什么信号。但注意：DES 用的是预测 top-50 vs 真实 top-50，也是一种"只看最强信号"的思路，可以期待 Top-k 类似的改进方式对 DES 也有帮助——这是下一步可以探索的方向。

---

## Slide 7 — 综合结论

### 内容

**四组实验的统一解读：**

```
信号确实存在，但稀疏
  └→ 集中在每个扰动里 |Wilcoxon scores| 最大的 3~6 个基因上
  └→ 对弱 DEG 没有方向预测能力

MMD 域对齐有真实贡献
  └→ Top-k k=3：STATE+MMD +0.099，Baseline -0.156，差距 0.26
  └→ MMD 纠正了 Baseline 在最强 DEG 上的系统性反向
  └→ 但 MMD 不解决 TabularLoss 对弱 DEG 无方向监督的问题

评估方法的选择决定能否看到信号
  └→ pval 阈值方法（两种阈值）：信号被稀释或被选择偏差遮蔽 → 结论"无能力"
  └→ Top-k 方法：聚焦强 DEG，覆盖全部扰动 → 结论"有限但真实的能力"
```

**下一步**：提高 logfc_weight（0.1 → 0.5），让方向损失的梯度真正影响 binary_decoder，再用 Top-k PCC 曲线验证提升效果。

### 讲稿

> 综合四组实验，我们现在对模型能力有了更清晰的认识。
>
> 模型不是完全没有方向预测能力——Top-k PCC 曲线明确显示了信号的存在。但这个信号是稀疏的，集中在最显著的少数基因上，对弱差异基因几乎没有预测能力。这是 TabularLoss 优化目标的根本局限。
>
> MMD 域对齐的贡献是真实的：它把 Baseline 在最强 DEG 上的系统性反向（k=3：-0.156）纠正成了正向（+0.099）。但 MMD 只作用在 CLS embedding 层，不能直接改变 binary_decoder 的方向监督信号。
>
> 最重要的方法论收获：用什么方式选基因、评估哪些扰动，会直接决定我们能不能发现模型的真实能力。Top-k 方法是目前最能揭示真实信号的评估方式，下一步所有实验都应该用它来衡量改进效果。

---

## 附录：重跑命令

### 实验一（pval < 0.20）重跑

```bash
cd /media/mldadmin/home/s125mdg34_03/state/src

python ../scripts/eval_deg_pearson.py \
    --checkpoint .../checkpoints/mmd_aae/last.ckpt \
    --baseline   .../checkpoints/no_mmd_baseline/last.ckpt \
    --config     ../configs/mmd_aae_config.yaml \
    --pval_cutoff 0.20 \
    --top_k_max  10
```

### 实验四（DES）重跑

```bash
cd /media/mldadmin/home/s125mdg34_03/state/src

python ../scripts/eval_des.py \
    --checkpoint .../checkpoints/mmd_aae/last.ckpt \
    --baseline   .../checkpoints/no_mmd_baseline/last.ckpt \
    --config     ../configs/mmd_aae_config.yaml
```

---

## 附录：今日实验完整数字（run2，2026-05-13）

### Top-k PCC — STATE+MMD

| k | mean PCC | median | std | N valid |
|---|---------|--------|-----|---------|
| 1 | NaN | NaN | NaN | 0 |
| 2 | 0.2239 | 1.0000 | 0.9746 | 67 |
| 3 | 0.0993 | 0.2490 | 0.7152 | 67 |
| 4 | 0.0879 | 0.0975 | 0.5569 | 67 |
| 5 | 0.1185 | 0.1953 | 0.4969 | 67 |
| 6 | 0.0795 | 0.0223 | 0.4313 | 67 |
| 7 | 0.0326 | 0.0085 | 0.4070 | 67 |
| 8 | 0.0081 | 0.0059 | 0.3832 | 67 |
| 9 | 0.0010 | 0.0055 | 0.3511 | 67 |
| 10 | 0.0320 | 0.0363 | 0.3235 | 67 |

### Top-k PCC — Baseline

| k | mean PCC | median | std | N valid |
|---|---------|--------|-----|---------|
| 1 | NaN | NaN | NaN | 0 |
| 2 | -0.0448 | -1.0000 | 0.9990 | 67 |
| 3 | -0.1559 | -0.3176 | 0.7132 | 67 |
| 4 | -0.0792 | -0.0978 | 0.5790 | 67 |
| 5 | -0.0552 | -0.0051 | 0.5009 | 67 |
| 6 | -0.0438 | 0.0070 | 0.4309 | 67 |
| 7 | -0.0437 | -0.0429 | 0.4203 | 67 |
| 8 | -0.0349 | 0.0315 | 0.4031 | 67 |
| 9 | -0.0445 | 0.0362 | 0.3729 | 67 |
| 10 | -0.0318 | 0.0595 | 0.3610 | 67 |

### pval < 0.05 — 今日

| 模型 | Mean PCC | Std | N（有效扰动） | 正相关比例 |
|------|---------|-----|------------|-----------|
| STATE+MMD | 0.0411 | 0.2049 | 39/68 | 53.8% |
| Baseline | 0.0460 | 0.2989 | 39/68 | 59.0% |
